let direction: "up" | "down" | "left" | "right";
direction = "down";

console.log(direction);

// direction = "bottom"; // Not Allowed

// Difference Between "literalType" and "enum"
// enum: creates a run-time js object
// literalType: basic validation to choose from
